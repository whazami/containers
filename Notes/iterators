On doit faire:

- iterator					(vector, map)
- const_iterator			(vector, map)
- reverse_iterator			(vector, map)
- const_reverse_iterator	(vector, map)
- iterator_traits

vector have random access iterators
map have bidirectionnal iterators


iterator_traits est un moyen de recuperer les informations
d'un iterator et a priori c'est tout.

enable_if permet d'autoriser ou interdire certains
overloads (de types) (apparemment conditionnels) a la compilation.
enable_if est une metafonction, cela signifie que le
resultat de cette fonction est obtenu et interprete a
la compilation (et non a l'execution), une metafonction
est une struct.

is_integral test juste si la class passee en parametre est un
entier (son attribut value est set a true).

equal test si une range (de first1 a last1) est egale a une
autre range (a partir de first2).

lexicographical_compare compare range1 et range2, si range2 est plus
petite ou si le premier element pas egal a range1 et plus petit, c'est false
et vice-versa.

pair et make_pair c'est pour map.
